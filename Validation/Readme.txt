The validation is for Grid search with labeled data. 
this hybrid code can easily changed.
Fist use Trend_Analysis_bbsmk to achive Tau for each case, then add these values to the target script and after setting paths, run it.